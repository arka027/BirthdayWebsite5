<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device‑width, initial‑scale=1.0" />
  <title>Happy Birthday Sis!</title>
  <style>
    * {
      box‑sizing: border‑box;
      margin: 0;
      padding: 0;
    }
    body {
      font‑family: "Segoe UI", sans‑serif;
      background: linear‑gradient(135deg, #ff9a9e, #fad0c4);
      color: #333;
      min‑height: 100vh;
      display: flex;
      flex‑direction: column;
      align‑items: center;
      padding: 20px;
    }
    .title {
      font‑size: 2.2rem;
      margin: 20px 0 30px;
      color: #fff;
      text‑shadow: 0 2px 10px rgba(0,0,0,0.3);
      text‑align: center;
    }
    .presents {
      display: flex;
      flex‑wrap: wrap;
      gap: 15px;
      justify‑content: center;
      max‑width: 600px;
    }
    .present {
      width: 100px;
      height: 80px;
      border‑radius: 12px;
      background: #ff4e50;
      display: flex;
      align‑items: center;
      justify‑content: center;
      color: #fff;
      font‑size: 1.2rem;
      font‑weight: bold;
      cursor: pointer;
      box‑shadow: 0 6px 15px rgba(0,0,0,0.2);
      transition: transform 0.3s, box‑shadow 0.3s;
    }
    .present:hover,
    .present:focus {
      transform: translateY(-5px) scale(1.05);
      box‑shadow: 0 10px 20px rgba(0,0,0,0.3);
    }
    .message {
      background: rgba(255,255,255,0.95);
      padding: 20px;
      border‑radius: 16px;
      box‑shadow: 0 6px 20px rgba(0,0,0,0.2);
      margin: 15px 20px;
      max‑width: 400px;
      text‑align: center;
      display: none;
      animation: fadeIn 0.4s;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    .message.active {
      display: block;
    }
    .back-btn {
      margin-top: 10px;
      padding: 8px 16px;
      background: #ff4e50;
      color: white;
      border: none;
      border‑radius: 20px;
      cursor: pointer;
      font‑size: 0.95rem;
    }
    .back-btn:hover {
      background: #e83a3c;
    }
    .confetti {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer‑events: none;
      z‑index: 1;
    }
    .confetti‑piece {
      position: absolute;
      width: 10px;
      height: 10px;
      border‑radius: 50%;
      opacity: 0.8;
    }
  </style>
</head>
<body>

  <h1 class="title">Happy 14th Birthday, <span id="sister">Sister!</span></h1>

  <div class="presents">
    <div class="present" data‑msg="1">🎁</div>
    <div class="present" data‑msg="2">🎉</div>
    <div class="present" data‑msg="3">🎂</div>
    <div class="present" data‑msg="4">✨</div>
    <div class="present" data‑msg="5">💖</div>
  </div>

  <div id="msg1" class="message">
    <p>Happy birthday, pagli! May your day be filled with laughter, yummy cake, and all your favourite things.</p>
    <button class="back-btn" onclick="hideMessage('msg1')">Back</button>
  </div>
  <div id="msg2" class="message">
    <p>You’re growing up so fast and you’re already such a kind, funny, and smart person. I’m proud to be your brother.</p>
    <button class="back-btn" onclick="hideMessage('msg2')">Back</button>
  </div>
  <div id="msg3" class="message">
    <p>Here’s wishing you all the best in your 14th year: new adventures, awesome friends, and a lot of happy memories.</p>
    <button class="back-btn" onclick="hideMessage('msg3')">Back</button>
  </div>
  <div id="msg4" class="message">
    <p>Never forget how special you are. You bring so much joy into our home and our family.</p>
    <button class="back-btn" onclick="hideMessage('msg4')">Back</button>
  </div>
  <div id="msg5" class="message">
    <p>Love you lots, sis! May all your dreams slowly come true. 🥰</p>
    <button class="back-btn" onclick="hideMessage('msg5')">Back</button>
  </div>

  <div class="confetti" id="confetti"></div>

  <script>
    // Customize name of your sister (optional)
    document.getElementById("sister").textContent = "Kuhu"; 

    function showMessage(id) {
      hideAllMessages();
      document.getElementById(id).classList.add("active");
      createConfetti();
    }

    function hideMessage(id) {
      document.getElementById(id).classList.remove("active");
    }

    function hideAllMessages() {
      document.querySelectorAll(".message").forEach(el => {
        el.classList.remove("active");
      });
    }

    function createConfetti() {
      const colors = ["#ff4e50", "#f9d423", "#00b4db", "#a8ff78", "#ff6b6b"];
      const confetti = document.getElementById("confetti");
      confetti.innerHTML = "";

      for (let i = 0; i < 100; i++) {
        const piece = document.createElement("div");
        piece.classList.add("confetti‑piece");
        piece.style.background = colors[Math.floor(Math.random() * colors.length)];
        piece.style.left = Math.random() * 100 + "%";
        piece.style.top = Math.random() * 100 + "%";
        piece.style.transform = `scale(${0.5 + Math.random()})`;
        confetti.appendChild(piece);
      }

      setTimeout(() => {
        confetti.innerHTML = "";
      }, 3000);
    }

    // bind clicks to each present
    document.querySelectorAll(".present").forEach(btn => {
      btn.addEventListener("click", () => {
        const msgId = "msg" + btn.getAttribute("data‑msg");
        showMessage(msgId);
      });
    });
  </script>

</body>
</html>